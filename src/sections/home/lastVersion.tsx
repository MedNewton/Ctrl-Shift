import { Stack, Typography } from "@mui/material";
import theme from "@/theme/theme";

const LastVersion = () => {
  return (
    <Stack>
      <Typography variant="h1">Last Version</Typography>
    </Stack>
  );
};

export default LastVersion;